# Puerto del servidor (por defecto 3333)
PORT=3333

# URI de MongoDB. Obligatorio para persistencia real (auth, propiedades, etc.).
MONGO_URI=mongodb://127.0.0.1:27017/airbnb

# true o 1 = persistencia solo en memoria (mocks). Para usar MongoDB deja en false o no definas.
USE_MEMORY_ONLY=false

# Secreto para firmar JWT (obligatorio). Usar un valor seguro en producción.
JWT_SECRET=change_me

# Caducidad del accessToken (ej. 7d, 24h)
JWT_EXPIRES_IN=7d

# Origen permitido para CORS (frontend). Vacío = permitir cualquier origen.
FRONTEND_ORIGIN=http://localhost:3000

# development | production. En production sin USE_MEMORY_ONLY no se exponen rutas /api/dev
# NODE_ENV=development
